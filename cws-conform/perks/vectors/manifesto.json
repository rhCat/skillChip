{
  "_perk": "vectors",
  "sequence": ["cws_vectors"],
  "tools": {"cws_vectors": {"binary": "python3", "params": {"CORPUS": "${CORPUS}"}}},
  "env": {"CORPUS": "${CORPUS}", "SIG_CORPUS": "${SIG_CORPUS}", "MIN_TOTAL": "${MIN_TOTAL}", "RECORD_STORE": "${record_store}"},
  "requires": ["python3"]
}

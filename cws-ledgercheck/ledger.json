{
  "_schema": "task-ledger TEMPLATE for cws-ledgercheck — set perk, fill record_store + the vars the perk's manifesto declares. The LLM outputs ONLY these fields.",
  "skill": "cws-ledgercheck",
  "perk": "verify",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "TARGET_LEDGER": "<path to the run-ledger.json to verify>"
  }
}

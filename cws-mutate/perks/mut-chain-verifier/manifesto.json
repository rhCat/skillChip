{
  "_perk": "mut-chain-verifier",
  "sequence": [
    "cws_chainverify"
  ],
  "tools": {
    "cws_chainverify": {
      "binary": "python3",
      "params": {
        "PROJECT_DIR": "${PROJECT_DIR}"
      }
    }
  },
  "env": {
    "PROJECT_DIR": "${PROJECT_DIR}",
    "THRESHOLD": "${THRESHOLD}",
    "MAX_MUTANTS": "${MAX_MUTANTS}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

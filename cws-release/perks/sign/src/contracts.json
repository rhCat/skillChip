{
  "tool": "cws_release_sign",
  "inputs": {},
  "outputs": {
    "release_out": {
      "path": "${RECORD_STORE}/release.json",
      "type": "file"
    }
  },
  "checks": {
    "exit_zero": true,
    "output_exists": "${RECORD_STORE}/release.json"
  }
}

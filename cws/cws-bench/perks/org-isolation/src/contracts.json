{
  "tool": "bench_org_isolation",
  "inputs": {},
  "outputs": {"org_isolation_out": {"path": "${RECORD_STORE}/org_isolation.json", "type": "file"}},
  "checks": {"exit_zero": true, "output_exists": "${RECORD_STORE}/org_isolation.json"}
}

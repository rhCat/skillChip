{
  "tool": "bench_store_reconcile",
  "inputs": {},
  "outputs": {"reconcile_out": {"path": "${RECORD_STORE}/reconcile.json", "type": "file"}},
  "checks": {"exit_zero": true, "output_exists": "${RECORD_STORE}/reconcile.json"}
}

{
  "tool": "cws_vectors",
  "inputs": {
    "CORPUS": {"type": "string", "required": true},
    "SIG_CORPUS": {"type": "string", "required": false},
    "MIN_TOTAL": {"type": "string", "required": false}
  },
  "outputs": {"conformance_out": {"path": "${RECORD_STORE}/conformance.json", "type": "file"}},
  "checks": {"exit_zero": true, "output_exists": "${RECORD_STORE}/conformance.json"}
}

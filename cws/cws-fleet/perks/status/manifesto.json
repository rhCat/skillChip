{
  "_perk": "status",
  "sequence": ["fleet_status"],
  "tools": {
    "fleet_status": {
      "binary": "python3",
      "params": {"FLEET_FILE": "${FLEET_FILE}"}
    }
  },
  "env": {
    "FLEET_FILE": "${FLEET_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": ["python3"]
}

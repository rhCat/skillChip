{
  "tool": "fleet_status",
  "inputs": {
    "FLEET_FILE": {"type": "string", "required": false}
  },
  "outputs": {
    "fleet_status_out": {"path": "${RECORD_STORE}/status.json", "type": "file"}
  },
  "checks": {
    "exit_zero": true,
    "output_exists": "${RECORD_STORE}/status.json"
  }
}

{
  "skill": "cws-mutate",
  "perks": [
    {
      "id": "mutate",
      "summary": "mutate a gate's source, run its test slice, score the survivors (V-MUT)",
      "destructive": false,
      "tools": [
        "cws_mutate"
      ]
    },
    {
      "id": "mut-chain-verifier",
      "summary": "R3 mutation gate PINNED to infra/cwp/chainverify.py (Ledger-v2 chain verifier) \u2014 score must stay >= 0.90 (P1-T10, SV-2)",
      "destructive": false,
      "tools": [
        "cws_chainverify"
      ]
    },
    {
      "id": "mut-snippet-verify",
      "summary": "R3 mutation gate PINNED to infra/govern/snippetverify.py (per-step snippet TOCTOU) \u2014 score must stay >= 0.90 (P1-T10, SV-2)",
      "destructive": false,
      "tools": [
        "cws_snippetverify"
      ]
    },
    {
      "id": "mut-grant-verify",
      "summary": "R3 mutation gate PINNED to infra/exec/grantverify.py (the Ed25519-DSSE grant verifier) \u2014 score >= 0.90 (SV-3 spine)",
      "destructive": false,
      "tools": [
        "cws_grantverify"
      ]
    },
    {
      "id": "mut-emitter",
      "summary": "mutation-test the workflow->TLA+ emitter (P4-T07) \u2014 mutate infra/cwp/tla_emit.py vs the golden slice; emitter_mutation_score >=0.90",
      "destructive": false,
      "tools": [
        "cws_mut_emitter"
      ]
    }
  ]
}

{
  "_schema": "task-ledger TEMPLATE for backbone \u2014 set perk, fill record_store + the vars the perk's manifesto declares.",
  "skill": "backbone",
  "perk": "validate",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "INPUT": "<value>"
  }
}

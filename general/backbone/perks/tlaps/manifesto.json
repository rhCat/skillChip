{
  "_perk": "tlaps",
  "sequence": [
    "bb_tlaps"
  ],
  "tools": {
    "bb_tlaps": {
      "binary": "python3",
      "params": {
        "TARGET_BLUEPRINT": "${TARGET_BLUEPRINT}",
        "TLAPM_TIMEOUT": "${TLAPM_TIMEOUT}"
      }
    }
  },
  "env": {
    "TARGET_BLUEPRINT": "${TARGET_BLUEPRINT}",
    "TLAPM_TIMEOUT": "${TLAPM_TIMEOUT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "tlapm"
  ]
}

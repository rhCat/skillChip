{
  "_schema": "task-ledger TEMPLATE for fs \u2014 set perk, fill record_store + the vars the perk's manifesto declares. The LLM outputs ONLY these fields.",
  "skill": "fs",
  "perk": "archive",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "SOURCE_DIR": "<source_dir>"
  }
}

{
  "_perk": "links",
  "sequence": [
    "md_links"
  ],
  "tools": {
    "md_links": {
      "binary": "python3",
      "params": {
        "MD_FILE": "${MD_FILE}"
      }
    }
  },
  "env": {
    "MD_FILE": "${MD_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

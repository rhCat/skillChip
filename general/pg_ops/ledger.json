{
  "_schema": "task-ledger TEMPLATE \u2014 copy, set perk, fill record_store + the vars the chosen perk's manifesto declares, then submit to validator\u2192composer\u2192compiler. The LLM outputs ONLY these fields.",
  "skill": "pg_ops",
  "perk": "select",
  "record_store": "<absolute dir for outputs + the run-ledger>",
  "vars": {
    "PGHOST": "<host>",
    "PGPORT": "5432",
    "PGDATABASE": "<db>",
    "PGUSER": "<user>",
    "PGPASSWORD_FILE": "<absolute path to a pass file, chmod 600 — read via cat at runtime; NEVER the password itself>",
    "QUERY": "<SELECT ...>",
    "LIMIT": "100"
  }
}

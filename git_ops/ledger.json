{
  "_schema": "task-ledger TEMPLATE for git_ops \u2014 set perk, fill record_store + the vars the perk's manifesto declares. The LLM outputs ONLY these fields.",
  "skill": "git_ops",
  "perk": "snapshot",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "REPO_DIR": "<repo_dir>",
    "MESSAGE": "<message>"
  }
}

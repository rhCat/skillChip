{
  "perk": "read",
  "skill": "toolgate",
  "description": "effect-class claim: a read-only tool call (read_file, search, snapshot) \u2014 non-destructive, verdict-only",
  "rules": [
    "verdict-only: govd decides; Hermes executes its own handler on allow \u2014 exod never runs the claimed tool",
    "value-free: TOOL is a name, ARGS_DIGEST a sha256, TARGET a coarse class label \u2014 argument values and secrets never cross the wire",
    "destructive perks resolve through push_back -> human approval; 'selfmod' additionally must never be ACL-loosened"
  ],
  "usage": "Claimed by the Hermes govern_gate for every tool call it classifies as 'read'. Vars: TOOL, ARGS_DIGEST, TARGET.",
  "limitation": "Records the claim; enforcement is the govd verdict plus Hermes' own gate chokepoint. This perk cannot observe or stop the tool itself.",
  "minimal_example": {
    "perk": "read",
    "vars": {
      "TOOL": "read_file",
      "ARGS_DIGEST": "<sha256-of-args>",
      "TARGET": "path:workzone"
    }
  }
}

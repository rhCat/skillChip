{
  "_perk": "extract_metadata",
  "sequence": [
    "extract_metadata"
  ],
  "tools": {
    "extract_metadata": {
      "binary": "python3",
      "params": {
        "IDENTIFIER": "${IDENTIFIER}"
      }
    }
  },
  "env": {
    "IDENTIFIER": "${IDENTIFIER}",
    "ID_FILE": "${ID_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "classify",
  "sequence": [
    "biomarker_classifier"
  ],
  "tools": {
    "biomarker_classifier": {
      "binary": "python3",
      "params": {
        "INPUT_CSV": "${INPUT_CSV}",
        "BIOMARKER": "${BIOMARKER}",
        "THRESHOLD": "${THRESHOLD}"
      }
    }
  },
  "env": {
    "INPUT_CSV": "${INPUT_CSV}",
    "BIOMARKER": "${BIOMARKER}",
    "THRESHOLD": "${THRESHOLD}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

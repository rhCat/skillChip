{
  "_perk": "schematic",
  "sequence": [
    "schematic"
  ],
  "tools": {
    "schematic": {
      "binary": "python3",
      "params": {
        "PROMPT": "${PROMPT}",
        "DOC_TYPE": "${DOC_TYPE}"
      }
    }
  },
  "env": {
    "PROMPT": "${PROMPT}",
    "DOC_TYPE": "${DOC_TYPE}",
    "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

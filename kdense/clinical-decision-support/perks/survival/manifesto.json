{
  "_perk": "survival",
  "sequence": [
    "survival_analysis"
  ],
  "tools": {
    "survival_analysis": {
      "binary": "python3",
      "params": {
        "INPUT_CSV": "${INPUT_CSV}",
        "TITLE": "${TITLE}"
      }
    }
  },
  "env": {
    "INPUT_CSV": "${INPUT_CSV}",
    "TITLE": "${TITLE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

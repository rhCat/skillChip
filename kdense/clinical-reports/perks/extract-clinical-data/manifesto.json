{
  "_perk": "extract-clinical-data",
  "sequence": [
    "extract_clinical_data"
  ],
  "tools": {
    "extract_clinical_data": {
      "binary": "python3",
      "params": {
        "REPORT_FILE": "${REPORT_FILE}"
      }
    }
  },
  "env": {
    "REPORT_FILE": "${REPORT_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "generate-schematic",
  "sequence": [
    "generate_schematic"
  ],
  "tools": {
    "generate_schematic": {
      "binary": "python3",
      "params": {
        "SCHEMATIC_PROMPT": "${SCHEMATIC_PROMPT}",
        "DOC_TYPE": "${DOC_TYPE}"
      }
    }
  },
  "env": {
    "SCHEMATIC_PROMPT": "${SCHEMATIC_PROMPT}",
    "DOC_TYPE": "${DOC_TYPE}",
    "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

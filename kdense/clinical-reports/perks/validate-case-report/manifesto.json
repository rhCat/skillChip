{
  "_perk": "validate-case-report",
  "sequence": [
    "validate_case_report"
  ],
  "tools": {
    "validate_case_report": {
      "binary": "python3",
      "params": {
        "REPORT_FILE": "${REPORT_FILE}"
      }
    }
  },
  "env": {
    "REPORT_FILE": "${REPORT_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

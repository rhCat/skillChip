{
  "tool": "validate_csv",
  "inputs": {
    "INPUT_CSV": {
      "type": "string",
      "required": true
    },
    "BASE_DIR": {
      "type": "string",
      "required": false
    }
  },
  "outputs": {
    "validation_report": {
      "path": "${RECORD_STORE}/validation_report.txt",
      "type": "file"
    }
  },
  "checks": {
    "exit_zero": true,
    "output_exists": "${RECORD_STORE}/validation_report.txt"
  }
}

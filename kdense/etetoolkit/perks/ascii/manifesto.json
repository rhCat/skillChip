{
  "_perk": "ascii",
  "sequence": [
    "ete_ascii"
  ],
  "tools": {
    "ete_ascii": {
      "binary": "python3",
      "params": {
        "TREE_FILE": "${TREE_FILE}",
        "TREE_FORMAT": "${TREE_FORMAT}"
      }
    }
  },
  "env": {
    "TREE_FILE": "${TREE_FILE}",
    "TREE_FORMAT": "${TREE_FORMAT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

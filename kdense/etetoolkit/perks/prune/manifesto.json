{
  "_perk": "prune",
  "sequence": [
    "ete_prune"
  ],
  "tools": {
    "ete_prune": {
      "binary": "python3",
      "params": {
        "TREE_FILE": "${TREE_FILE}",
        "KEEP_TAXA": "${KEEP_TAXA}",
        "TREE_FORMAT": "${TREE_FORMAT}"
      }
    }
  },
  "env": {
    "TREE_FILE": "${TREE_FILE}",
    "KEEP_TAXA": "${KEEP_TAXA}",
    "TREE_FORMAT": "${TREE_FORMAT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

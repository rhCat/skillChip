{
  "_perk": "reroot",
  "sequence": [
    "ete_reroot"
  ],
  "tools": {
    "ete_reroot": {
      "binary": "python3",
      "params": {
        "TREE_FILE": "${TREE_FILE}",
        "OUTGROUP": "${OUTGROUP}",
        "TREE_FORMAT": "${TREE_FORMAT}"
      }
    }
  },
  "env": {
    "TREE_FILE": "${TREE_FILE}",
    "OUTGROUP": "${OUTGROUP}",
    "TREE_FORMAT": "${TREE_FORMAT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

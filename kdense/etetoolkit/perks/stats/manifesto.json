{
  "_perk": "stats",
  "sequence": [
    "ete_stats"
  ],
  "tools": {
    "ete_stats": {
      "binary": "python3",
      "params": {
        "TREE_FILE": "${TREE_FILE}",
        "TREE_FORMAT": "${TREE_FORMAT}"
      }
    }
  },
  "env": {
    "TREE_FILE": "${TREE_FILE}",
    "TREE_FORMAT": "${TREE_FORMAT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

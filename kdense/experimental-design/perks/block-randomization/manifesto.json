{
  "_perk": "block-randomization",
  "sequence": [
    "block_randomization"
  ],
  "tools": {
    "block_randomization": {
      "binary": "python3",
      "params": {
        "SPEC_JSON": "${SPEC_JSON}"
      }
    }
  },
  "env": {
    "SPEC_JSON": "${SPEC_JSON}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

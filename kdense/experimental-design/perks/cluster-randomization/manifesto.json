{
  "_perk": "cluster-randomization",
  "sequence": [
    "cluster_randomization"
  ],
  "tools": {
    "cluster_randomization": {
      "binary": "python3",
      "params": {
        "SPEC_JSON": "${SPEC_JSON}"
      }
    }
  },
  "env": {
    "SPEC_JSON": "${SPEC_JSON}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "fractional-factorial",
  "sequence": [
    "fractional_factorial"
  ],
  "tools": {
    "fractional_factorial": {
      "binary": "python3",
      "params": {
        "SPEC_JSON": "${SPEC_JSON}"
      }
    }
  },
  "env": {
    "SPEC_JSON": "${SPEC_JSON}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

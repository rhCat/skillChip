{
  "_perk": "stratified-block-randomization",
  "sequence": [
    "stratified_block_randomization"
  ],
  "tools": {
    "stratified_block_randomization": {
      "binary": "python3",
      "params": {
        "SPEC_JSON": "${SPEC_JSON}"
      }
    }
  },
  "env": {
    "SPEC_JSON": "${SPEC_JSON}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

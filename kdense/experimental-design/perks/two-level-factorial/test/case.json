{
  "_perk": "two-level-factorial",
  "requires": ["python3"],
  "vars": { "SPEC_JSON": "${FIXTURE}/spec.json" },
  "expect": {
    "exit": 0,
    "outputs": ["experimental_runs.csv"],
    "nonempty": ["experimental_runs.csv"]
  }
}

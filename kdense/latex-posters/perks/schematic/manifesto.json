{
  "_perk": "schematic",
  "sequence": [
    "generate_schematic"
  ],
  "tools": {
    "generate_schematic": {
      "binary": "python3",
      "params": {
        "PROMPT": "${PROMPT}",
        "OUTPUT_NAME": "${OUTPUT_NAME}",
        "DOC_TYPE": "${DOC_TYPE}"
      }
    }
  },
  "env": {
    "PROMPT": "${PROMPT}",
    "OUTPUT_NAME": "${OUTPUT_NAME}",
    "DOC_TYPE": "${DOC_TYPE}",
    "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

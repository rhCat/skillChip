{
  "_perk": "chat_session_create",
  "sequence": [
    "chat_session_create"
  ],
  "tools": {
    "chat_session_create": {
      "binary": "bash",
      "params": {
        "OPEN_NOTEBOOK_URL": "${OPEN_NOTEBOOK_URL}",
        "NOTEBOOK_ID": "${NOTEBOOK_ID}",
        "SESSION_TITLE": "${SESSION_TITLE}",
        "MODEL_OVERRIDE": "${MODEL_OVERRIDE}"
      }
    }
  },
  "env": {
    "OPEN_NOTEBOOK_URL": "${OPEN_NOTEBOOK_URL}",
    "NOTEBOOK_ID": "${NOTEBOOK_ID}",
    "SESSION_TITLE": "${SESSION_TITLE}",
    "MODEL_OVERRIDE": "${MODEL_OVERRIDE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

{
  "_perk": "extract",
  "sequence": [
    "extract"
  ],
  "tools": {
    "extract": {
      "binary": "python3",
      "params": {
        "PPTX_FILE": "${PPTX_FILE}"
      }
    }
  },
  "env": {
    "PPTX_FILE": "${PPTX_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

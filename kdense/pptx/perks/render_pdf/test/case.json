{
  "_perk": "render_pdf",
  "requires": ["python3"],
  "vars": {
    "PPTX_FILE": "${FIXTURE}/sample.pptx"
  },
  "expect": {
    "exit": 0,
    "outputs": ["render.log"],
    "nonempty": ["render.log"]
  }
}

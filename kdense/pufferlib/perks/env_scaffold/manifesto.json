{
  "_perk": "env_scaffold",
  "sequence": [
    "env_scaffold"
  ],
  "tools": {
    "env_scaffold": {
      "binary": "python3",
      "params": {
        "GRID_SIZE": "${GRID_SIZE}"
      }
    }
  },
  "env": {
    "GRID_SIZE": "${GRID_SIZE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_schema": "task-ledger TEMPLATE for pymatgen — set perk, fill record_store + the vars the perk declares.",
  "skill": "pymatgen",
  "perk": "convert",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "INPUT_FILE": "<path to input structure file (CIF, POSCAR, XYZ, ...)>",
    "OUTPUT_FORMAT": "<target format, e.g. cif | poscar | xyz | json>"
  }
}

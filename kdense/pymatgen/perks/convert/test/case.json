{
  "_perk": "convert",
  "requires": ["python3"],
  "vars": {
    "INPUT_FILE": "${FIXTURE}/POSCAR",
    "OUTPUT_FORMAT": "cif"
  },
  "expect": {
    "exit": 0,
    "outputs": ["convert.log"],
    "nonempty": ["convert.log"]
  }
}

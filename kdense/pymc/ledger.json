{
  "_schema": "task-ledger TEMPLATE for pymc — set perk, fill record_store + the vars the perk declares.",
  "skill": "pymc",
  "perk": "diagnostics",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "IDATA": "<path to a PyMC/ArviZ InferenceData .nc file>"
  }
}

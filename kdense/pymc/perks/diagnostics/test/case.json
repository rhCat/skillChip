{
  "_perk": "diagnostics",
  "requires": ["bash", "python3"],
  "vars": { "IDATA": "${FIXTURE}/idata.nc", "VAR_NAMES": "alpha,beta,sigma" },
  "expect": {
    "exit": 0,
    "outputs": ["diagnostics.json"],
    "nonempty": ["diagnostics.json"]
  }
}

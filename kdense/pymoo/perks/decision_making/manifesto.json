{
  "_perk": "decision_making",
  "sequence": [
    "pymoo_decision_making"
  ],
  "tools": {
    "pymoo_decision_making": {
      "binary": "python3",
      "params": {
        "PROBLEM": "${PROBLEM}",
        "POP_SIZE": "${POP_SIZE}",
        "N_GEN": "${N_GEN}"
      }
    }
  },
  "env": {
    "PROBLEM": "${PROBLEM}",
    "POP_SIZE": "${POP_SIZE}",
    "N_GEN": "${N_GEN}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

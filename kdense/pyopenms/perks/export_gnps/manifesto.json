{
  "_perk": "export_gnps",
  "sequence": [
    "export_gnps"
  ],
  "tools": {
    "export_gnps": {
      "binary": "bash",
      "params": {
        "CONSENSUS": "${CONSENSUS}",
        "MZML": "${MZML}"
      }
    }
  },
  "env": {
    "CONSENSUS": "${CONSENSUS}",
    "MZML": "${MZML}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

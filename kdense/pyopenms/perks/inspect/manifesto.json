{
  "_perk": "inspect",
  "sequence": [
    "inspect_ms_data"
  ],
  "tools": {
    "inspect_ms_data": {
      "binary": "bash",
      "params": {
        "INPUT": "${INPUT}"
      }
    }
  },
  "env": {
    "INPUT": "${INPUT}",
    "SPECTRA_CSV": "${SPECTRA_CSV}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

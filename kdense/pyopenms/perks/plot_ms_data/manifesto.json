{
  "_perk": "plot_ms_data",
  "sequence": [
    "plot_ms_data"
  ],
  "tools": {
    "plot_ms_data": {
      "binary": "bash",
      "params": {
        "KIND": "${KIND}",
        "INPUT": "${INPUT}"
      }
    }
  },
  "env": {
    "KIND": "${KIND}",
    "INPUT": "${INPUT}",
    "INDEX": "${INDEX}",
    "RT": "${RT}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

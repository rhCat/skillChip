{
  "_perk": "scaffold_datamodule",
  "sequence": [
    "scaffold_datamodule"
  ],
  "tools": {
    "scaffold_datamodule": {
      "binary": "python3",
      "params": {
        "OUT_NAME": "${OUT_NAME}"
      }
    }
  },
  "env": {
    "OUT_NAME": "${OUT_NAME}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

{
  "_perk": "scaffold_module",
  "sequence": [
    "scaffold_module"
  ],
  "tools": {
    "scaffold_module": {
      "binary": "python3",
      "params": {
        "OUT_NAME": "${OUT_NAME}"
      }
    }
  },
  "env": {
    "OUT_NAME": "${OUT_NAME}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3",
    "bash"
  ]
}

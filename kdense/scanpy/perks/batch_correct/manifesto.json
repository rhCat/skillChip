{
  "_perk": "batch_correct",
  "sequence": [
    "batch_correct"
  ],
  "tools": {
    "batch_correct": {
      "binary": "python3",
      "params": {
        "INPUT": "${INPUT}",
        "BATCH_KEY": "${BATCH_KEY}"
      }
    }
  },
  "env": {
    "INPUT": "${INPUT}",
    "BATCH_KEY": "${BATCH_KEY}",
    "METHOD": "${METHOD}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_schema": "task-ledger TEMPLATE for sci-pdf — set perk, fill record_store + the vars the perk declares.",
  "skill": "sci-pdf",
  "perk": "check-fillable",
  "record_store": "<absolute dir for outputs + run-ledger>",
  "vars": {
    "PDF_PATH": "<input .pdf file>"
  }
}

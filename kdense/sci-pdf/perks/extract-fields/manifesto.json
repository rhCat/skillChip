{
  "_perk": "extract-fields",
  "sequence": [
    "extract_fields"
  ],
  "tools": {
    "extract_fields": {
      "binary": "python3",
      "params": {
        "PDF_PATH": "${PDF_PATH}"
      }
    }
  },
  "env": {
    "PDF_PATH": "${PDF_PATH}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

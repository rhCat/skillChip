{
  "_perk": "extract-structure",
  "sequence": [
    "extract_structure"
  ],
  "tools": {
    "extract_structure": {
      "binary": "python3",
      "params": {
        "PDF_PATH": "${PDF_PATH}"
      }
    }
  },
  "env": {
    "PDF_PATH": "${PDF_PATH}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

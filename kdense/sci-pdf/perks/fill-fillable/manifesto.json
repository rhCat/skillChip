{
  "_perk": "fill-fillable",
  "sequence": [
    "fill_fillable"
  ],
  "tools": {
    "fill_fillable": {
      "binary": "python3",
      "params": {
        "PDF_PATH": "${PDF_PATH}",
        "FIELDS_JSON": "${FIELDS_JSON}"
      }
    }
  },
  "env": {
    "PDF_PATH": "${PDF_PATH}",
    "FIELDS_JSON": "${FIELDS_JSON}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

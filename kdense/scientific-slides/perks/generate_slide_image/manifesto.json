{
  "_perk": "generate_slide_image",
  "sequence": [
    "generate_slide_image"
  ],
  "tools": {
    "generate_slide_image": {
      "binary": "python3",
      "params": {
        "PROMPT": "${PROMPT}",
        "VISUAL_ONLY": "${VISUAL_ONLY}"
      }
    }
  },
  "env": {
    "PROMPT": "${PROMPT}",
    "VISUAL_ONLY": "${VISUAL_ONLY}",
    "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

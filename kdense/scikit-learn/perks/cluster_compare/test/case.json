{
  "_perk": "cluster_compare",
  "requires": ["python3"],
  "vars": {
    "DATA_CSV": "${FIXTURE}/data.csv",
    "N_CLUSTERS": "3"
  },
  "expect": {
    "exit": 0,
    "outputs": ["cluster_compare.json"],
    "nonempty": ["cluster_compare.json"]
  }
}

{
  "_perk": "load",
  "sequence": [
    "load"
  ],
  "tools": {
    "load": {
      "binary": "python3",
      "params": {
        "INPUT": "${INPUT}"
      }
    }
  },
  "env": {
    "INPUT": "${INPUT}",
    "PROCESSED": "${PROCESSED}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "plot_embedding",
  "sequence": [
    "plot_embedding"
  ],
  "tools": {
    "plot_embedding": {
      "binary": "python3",
      "params": {
        "INPUT": "${INPUT}"
      }
    }
  },
  "env": {
    "INPUT": "${INPUT}",
    "KIND": "${KIND}",
    "BASIS": "${BASIS}",
    "COLOR": "${COLOR}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "check_completeness",
  "sequence": [
    "check_completeness"
  ],
  "tools": {
    "check_completeness": {
      "binary": "python3",
      "params": {
        "PLAN_FILE": "${PLAN_FILE}"
      }
    }
  },
  "env": {
    "PLAN_FILE": "${PLAN_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

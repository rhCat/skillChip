{
  "_perk": "generate_timeline",
  "sequence": [
    "generate_timeline"
  ],
  "tools": {
    "generate_timeline": {
      "binary": "python3",
      "params": {
        "PLAN_FILE": "${PLAN_FILE}"
      }
    }
  },
  "env": {
    "PLAN_FILE": "${PLAN_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

{
  "_perk": "generate_timeline",
  "requires": ["python3"],
  "vars": { "PLAN_FILE": "${FIXTURE}" },
  "expect": {
    "exit": 0,
    "outputs": ["timeline.txt"],
    "nonempty": ["timeline.txt"]
  }
}

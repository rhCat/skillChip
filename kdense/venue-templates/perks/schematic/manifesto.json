{
  "_perk": "schematic",
  "sequence": [
    "generate_schematic"
  ],
  "tools": {
    "generate_schematic": {
      "binary": "python3",
      "params": {
        "PROMPT": "${PROMPT}",
        "DOC_TYPE": "${DOC_TYPE}",
        "ITERATIONS": "${ITERATIONS}"
      }
    }
  },
  "env": {
    "PROMPT": "${PROMPT}",
    "DOC_TYPE": "${DOC_TYPE}",
    "ITERATIONS": "${ITERATIONS}",
    "OPENROUTER_API_KEY": "${OPENROUTER_API_KEY}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

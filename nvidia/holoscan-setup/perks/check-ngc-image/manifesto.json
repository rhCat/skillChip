{
  "_perk": "check-ngc-image",
  "sequence": [
    "check_ngc_image"
  ],
  "tools": {
    "check_ngc_image": {
      "binary": "bash",
      "params": {
        "CUDA_TAG_SUFFIX": "${CUDA_TAG_SUFFIX}"
      }
    }
  },
  "env": {
    "CUDA_TAG_SUFFIX": "${CUDA_TAG_SUFFIX}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "bash"
  ]
}

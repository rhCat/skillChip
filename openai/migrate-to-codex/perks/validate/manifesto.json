{
  "_perk": "validate",
  "sequence": [
    "mtc_validate"
  ],
  "tools": {
    "mtc_validate": {
      "binary": "python3",
      "params": {
        "TARGET": "${TARGET}"
      }
    }
  },
  "env": {
    "TARGET": "${TARGET}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

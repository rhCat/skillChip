{
  "_perk": "validate",
  "sequence": [
    "validate_skill"
  ],
  "tools": {
    "validate_skill": {
      "binary": "python3",
      "params": {
        "SKILL_DIR": "${SKILL_DIR}"
      }
    }
  },
  "env": {
    "SKILL_DIR": "${SKILL_DIR}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": [
    "python3"
  ]
}

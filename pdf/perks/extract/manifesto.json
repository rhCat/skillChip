{
  "_perk": "extract",
  "sequence": [
    "pdf_extract"
  ],
  "tools": {
    "pdf_extract": {
      "binary": "python3",
      "params": {
        "PDF_FILE": "${PDF_FILE}"
      }
    }
  },
  "env": {
    "PDF_FILE": "${PDF_FILE}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": []
}

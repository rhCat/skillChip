{
  "_perk": "grep",
  "requires": ["grep"],
  "vars": { "PATTERN": "def", "SEARCH_DIR": "${FIXTURE}" },
  "expect": {
    "outputs": ["matches.txt"],
    "contains": { "matches.txt": "def used" }
  }
}

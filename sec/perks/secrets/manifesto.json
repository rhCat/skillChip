{
  "_perk": "secrets",
  "sequence": [
    "sec_secrets"
  ],
  "tools": {
    "sec_secrets": {
      "binary": "grep",
      "params": {
        "SEARCH_DIR": "${SEARCH_DIR}"
      }
    }
  },
  "env": {
    "SEARCH_DIR": "${SEARCH_DIR}",
    "RECORD_STORE": "${record_store}"
  },
  "requires": []
}

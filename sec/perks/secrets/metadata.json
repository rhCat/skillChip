{
  "perk": "secrets",
  "skill": "sec",
  "description": "scan a tree for likely leaked secrets (grep, read-only)",
  "rules": [
    "read-only",
    "JSON report to record_store",
    "regex heuristic — may have false positives"
  ],
  "usage": "Set SEARCH_DIR. Output: secrets_report.json (always written; empty findings when none).",
  "limitation": "Line-oriented regex heuristics; no entropy analysis; one tree per run.",
  "minimal_example": {
    "perk": "secrets",
    "vars": {
      "SEARCH_DIR": "."
    }
  }
}

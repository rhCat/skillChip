{
  "_perk": "secrets",
  "requires": ["grep", "awk"],
  "vars": { "SEARCH_DIR": "${FIXTURE}" },
  "expect": {
    "outputs": ["secrets_report.json"],
    "json": { "secrets_report.json": { "count": 1 } }
  }
}
